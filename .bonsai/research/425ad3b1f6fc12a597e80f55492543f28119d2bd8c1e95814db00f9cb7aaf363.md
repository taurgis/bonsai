---
schema_version: 1
artifact_type: source
source_url: https://oclif.io/docs/json/
source_urls:
  - https://oclif.io/docs/json/
normalized_url: https://oclif.io/docs/json/
cache_key: 425ad3b1f6fc12a597e80f55492543f28119d2bd8c1e95814db00f9cb7aaf363
topic: CLI audit rule sources
tags:
  - cli
  - audit
format_available:
  - compressed
  - detailed
tier: stable
ttl: 
fetched_at: 2026-07-19T09:32:17.221Z
validated_at: 2026-07-19T09:32:17.221Z
stale_after: 2027-01-15T09:32:17.221Z
capture_method: static_fetch
extraction_status: extracted
extraction_confidence: medium
quality_notes:
  - readability extracted main article
supplied_at: 
supplied_by: 
etag: W/"6a58525e-6856"
last_modified: Thu, 16 Jul 2026 03:39:10 GMT
content_hash: 323ab103a75a143f18acc7ad6db544c9c6309d9c4c0ee49b722d49b831ff124a
token_estimate:
  compressed: 193
  detailed: 193
status: active
site_module_id: 
docs_engine: docusaurus
docs_framework: 
source_doc_url: 
search_provider: algolia-docsearch
parent_cache_key: 
section_anchor: 
section_heading_path: 
---

## Summary

JSON | oclif: The Open CLI Framework

## Compressed

If you want to use the `--json` flag to return JSON output to the user, then you can set the `enableJsonFlag` property on the `Command` class.

When this property is set and the user supplies the `--json` flag, the command will suppress all logs and instead log the return value to the console in JSON format. **Note** log suppression will only work if you use the logging methods on the `Command` class instance. In other words, `this.log` will be automatically suppressed but `console.log` will not be.

```typescript
import {Command} from '@oclif/core'
export class HelloCommand extends Command {
  public static enableJsonFlag = true
  public async run(): Promise<{ message: string }> {
    this.log('hello, world!')
    return { message: 'hello, world!' }
  }
}
```

## Detailed

If you want to use the `--json` flag to return JSON output to the user, then you can set the `enableJsonFlag` property on the `Command` class.

When this property is set and the user supplies the `--json` flag, the command will suppress all logs and instead log the return value to the console in JSON format. **Note** log suppression will only work if you use the logging methods on the `Command` class instance. In other words, `this.log` will be automatically suppressed but `console.log` will not be.

```typescript
import {Command} from '@oclif/core'
export class HelloCommand extends Command {
  public static enableJsonFlag = true
  public async run(): Promise<{ message: string }> {
    this.log('hello, world!')
    return { message: 'hello, world!' }
  }
}
```

## Provenance

Fetched from https://oclif.io/docs/json/ on 2026-07-19T09:32:17.221Z