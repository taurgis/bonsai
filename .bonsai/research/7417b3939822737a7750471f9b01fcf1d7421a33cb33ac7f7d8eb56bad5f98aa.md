---
schema_version: 1
artifact_type: section
source_url: https://code.claude.com/docs/en/memory#storage-location
source_urls:
  - https://code.claude.com/docs/en/memory#storage-location
normalized_url: https://code.claude.com/docs/en/memory
cache_key: 7417b3939822737a7750471f9b01fcf1d7421a33cb33ac7f7d8eb56bad5f98aa
topic: 
tags:
  - files
  - claude
  - claude.md
  - instructions
  - memory
format_available:
  - compressed
  - detailed
tier: standard
ttl: 
fetched_at: 2026-07-19T13:32:46.158Z
validated_at: 2026-07-19T13:32:46.158Z
stale_after: 2026-08-18T13:32:46.158Z
capture_method: route_markdown
extraction_status: extracted
extraction_confidence: high
quality_notes:
  - captured from public Markdown/MDX source: https://code.claude.com/docs/en/memory.md
  - auto-generated tags via keyword extraction
supplied_at: 
supplied_by: 
etag: 
last_modified: 
content_hash: 27af81234f07a092787f8238f1160deb4ac07dd2be63c3cfe7ca6edb4e937a11
token_estimate:
  compressed: 294
  detailed: 403
status: active
site_module_id: 
docs_engine: next
docs_framework: mintlify
source_doc_url: https://code.claude.com/docs/en/memory.md
search_provider: 
parent_cache_key: f1c064262dc87f8c9dd718b08754fbe0cf7c81670e65e46ababe1cbae7f782ab
section_anchor: storage-location
section_heading_path: How Claude remembers your project > Auto memory > Storage location
---

## Summary

How Claude remembers your project > Auto memory > Storage location

## Compressed

### Storage location

Each project gets its own memory directory at `~/.claude/projects/<project>/memory/`. The `<project>` path is derived from the git repository, so all worktrees and subdirectories within the same repo share one auto memory directory.

To store auto memory in a different location, set `autoMemoryDirectory` in your `settings.json`.

```json theme={null}
{
  "autoMemoryDirectory": "~/my-custom-memory-dir"
}
```

The value must be an absolute path or start with `~/`.

The directory contains a `MEMORY.md` entrypoint and optional topic files:

```text theme={null}
~/.claude/projects/<project>/memory/
├── MEMORY.md          # Concise index, loaded into every session
├── debugging.md       # Detailed notes on debugging patterns
├── api-conventions.md # API design decisions
└── ...                # Any other topic files Claude creates
```

`MEMORY.md` acts as an index of the memory directory. Claude reads and writes files in this directory throughout your session, using `MEMORY.md` to keep track of what's stored where.

Auto memory is machine-local. All worktrees and subdirectories within the same git repository share one auto memory directory.

## Detailed

### Storage location

Each project gets its own memory directory at `~/.claude/projects/<project>/memory/`. The `<project>` path is derived from the git repository, so all worktrees and subdirectories within the same repo share one auto memory directory. Outside a git repo, the project root is used instead.

To store auto memory in a different location, set `autoMemoryDirectory` in your `settings.json`. It is read from any [settings scope](/en/settings#settings-precedence): user, project, local, policy, or `--settings`.

```json theme={null}
{
  "autoMemoryDirectory": "~/my-custom-memory-dir"
}
```

The value must be an absolute path or start with `~/`. When set in a project's `.claude/settings.json` or `.claude/settings.local.json`, the value is honored only after you accept the workspace trust dialog for that folder, the same gate that governs hooks.

The directory contains a `MEMORY.md` entrypoint and optional topic files:

```text theme={null}
~/.claude/projects/<project>/memory/
├── MEMORY.md          # Concise index, loaded into every session
├── debugging.md       # Detailed notes on debugging patterns
├── api-conventions.md # API design decisions
└── ...                # Any other topic files Claude creates
```

`MEMORY.md` acts as an index of the memory directory. Claude reads and writes files in this directory throughout your session, using `MEMORY.md` to keep track of what's stored where.

Auto memory is machine-local. All worktrees and subdirectories within the same git repository share one auto memory directory. Files are not shared across machines or cloud environments.

## Provenance

Section "How Claude remembers your project > Auto memory > Storage location" of https://code.claude.com/docs/en/memory (parent f1c064262dc87f8c9dd718b08754fbe0cf7c81670e65e46ababe1cbae7f782ab)