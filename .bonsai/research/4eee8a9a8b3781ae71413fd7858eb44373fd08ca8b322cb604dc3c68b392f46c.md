---
schema_version: 1
artifact_type: section
source_url: https://cheatsheetseries.owasp.org/cheatsheets/Nodejs_Security_Cheat_Sheet.html#permissions
source_urls:
  - https://cheatsheetseries.owasp.org/cheatsheets/Nodejs_Security_Cheat_Sheet.html#permissions
normalized_url: https://cheatsheetseries.owasp.org/cheatsheets/Nodejs_Security_Cheat_Sheet.html
cache_key: 4eee8a9a8b3781ae71413fd7858eb44373fd08ca8b322cb604dc3c68b392f46c
topic: 
tags:
  - application
  - link
  - permanent
  - security
  - node.js
format_available:
  - compressed
  - detailed
tier: standard
ttl: 
fetched_at: 2026-07-19T10:01:33.105Z
validated_at: 2026-07-19T10:01:33.105Z
stale_after: 2026-08-18T10:01:33.105Z
capture_method: static_fetch
extraction_status: extracted
extraction_confidence: high
quality_notes:
  - readability extracted main article
  - quality:oversized
  - auto-generated tags via keyword extraction
supplied_at: 
supplied_by: 
etag: 
last_modified: Fri, 17 Jul 2026 14:52:54 GMT
content_hash: 0f454cca344fcdf06b3377607ce0d371b7c22593a16ea0b848ba3726f1af28ff
token_estimate:
  compressed: 320
  detailed: 492
status: active
site_module_id: 
docs_engine: generated-static
docs_framework: material-mkdocs
source_doc_url: 
search_provider: 
parent_cache_key: 4da32170976258dc6be0fa04dce1bd3dfea31d9aac26c7be42a09927a5d7b659
section_anchor: permissions
section_heading_path: Recommendations¶ > Permissions¶
---

## Summary

Recommendations¶ > Permissions¶

## Compressed

### Permissions[¶]

Starting with Node.js v20, a Permission Model is available to restrict application privileges.

#### Usage[¶]

```
node --permission [--allow-<type>=...] app.js
```

#### Examples[¶]

Use only `--permission` to restrict all permissions by default:

```
node --permission index.js
```

Use `--allow‑fs‑read` to specify which files or directories Node.js is allowed to read.

```
node --permission --allow-fs-read=/uploads/ index.js
```

Use `--allow-fs-write` to specify where Node.js is allowed to write files:

```
node --permission --allow-fs-write=/uploads/ index.js
```

Unauthorized access triggers a runtime error:

```
Error: Access to this API has been restricted
code: 'ERR_ACCESS_DENIED'
permission: 'FileSystemRead'
resource: '/path/to/file'
```

Other flags:

*   `--allow-child-process` — allows using `child_process` APIs
*   `--allow-worker` — allows usage of `worker_threads` APIs
*   `--allow-addons` — enables loading native addons
*   `--allow-wasi` — enables usage of WASI modules

**Important note:** Symbolic links are followed even if they point outside the allowed paths. To stay secure make sure no allowed paths include relative symbolic links when using the permission model.

For more details refer to the [official documentation].

## Detailed

### Permissions[¶](https://cheatsheetseries.owasp.org/cheatsheets/Nodejs_Security_Cheat_Sheet.html#permissions "Permanent link")

Starting with Node.js v20, a Permission Model is available to restrict application privileges. As of Node.js v23.5.0, the model is considered stable and can be enabled using the `--permission` flag (earlier versions require `--experimental-permission`).

#### Usage[¶](https://cheatsheetseries.owasp.org/cheatsheets/Nodejs_Security_Cheat_Sheet.html#usage "Permanent link")

```
node --permission [--allow-<type>=...] app.js
```

#### Examples[¶](https://cheatsheetseries.owasp.org/cheatsheets/Nodejs_Security_Cheat_Sheet.html#examples "Permanent link")

Use only `--permission` to restrict all permissions by default:

```
node --permission index.js
```

Use `--allow‑fs‑read` to specify which files or directories Node.js is allowed to read. This is especially useful to prevent Local File Inclusion (LFI) vulnerabilities:

```
node --permission --allow-fs-read=/uploads/ index.js
```

Use `--allow-fs-write` to specify where Node.js is allowed to write files:

```
node --permission --allow-fs-write=/uploads/ index.js
```

Unauthorized access triggers a runtime error:

```
Error: Access to this API has been restricted
code: 'ERR_ACCESS_DENIED'
permission: 'FileSystemRead'
resource: '/path/to/file'
```

Other flags:

*   `--allow-child-process` — allows using `child_process` APIs
*   `--allow-worker` — allows usage of `worker_threads` APIs
*   `--allow-addons` — enables loading native addons
*   `--allow-wasi` — enables usage of WASI modules

**Important note:** Symbolic links are followed even if they point outside the allowed paths. This means that relative symlinks can bypass restrictions and grant unintended access. To stay secure make sure no allowed paths include relative symbolic links when using the permission model.

For more details refer to the [official documentation](https://nodejs.org/api/permissions.html).

## Provenance

Section "Recommendations¶ > Permissions¶" of https://cheatsheetseries.owasp.org/cheatsheets/Nodejs_Security_Cheat_Sheet.html (parent 4da32170976258dc6be0fa04dce1bd3dfea31d9aac26c7be42a09927a5d7b659)