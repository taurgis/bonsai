---
schema_version: 1
artifact_type: section
source_url: https://cursor.com/docs/hooks#project-hooks-cursor
source_urls:
  - https://cursor.com/docs/hooks#project-hooks-cursor
normalized_url: https://cursor.com/docs/hooks
cache_key: fa2051069c8c768cfef8b10cd6213653e090a089bea63e1765ef718f9bed3c29
topic: 
tags:
  - hooks
  - string
  - hook
  - agent
  - type
format_available:
  - compressed
  - detailed
tier: standard
ttl: 
fetched_at: 2026-06-26T17:04:16.722Z
validated_at: 2026-06-26T17:04:16.722Z
stale_after: 2026-07-26T17:04:16.722Z
capture_method: route_markdown
extraction_status: extracted
extraction_confidence: high
quality_notes:
  - captured from public Markdown/MDX source: https://cursor.com/docs/hooks.md
  - auto-generated tags via keyword extraction
supplied_at: 
supplied_by: 
etag: W/"7023b9f712221bcd023a2a8deaba1c74"
last_modified: 
content_hash: a663e8ba05a9aaf008ba50ed306106427f04e2493f3021f1147ff1dc0a2b8ae0
token_estimate:
  compressed: 169
  detailed: 169
status: active
site_module_id: 
docs_engine: next
docs_framework: 
source_doc_url: https://cursor.com/docs/hooks.md
search_provider: algolia-docsearch
parent_cache_key: 5f6188ddb87cb68f4a982b8392650fc7591cfe85021b4dcce56657c421800b46
section_anchor: project-hooks-cursor
section_heading_path: Hooks > Quickstart > Project hooks (.cursor/)
---

## Summary

Hooks > Quickstart > Project hooks (.cursor/)

## Compressed

### Project hooks (.cursor/)

For project-level hooks that apply to a specific repository, create `<project>/.cursor/hooks.json`:

```json
{
  "version": 1,
  "hooks": {
    "afterFileEdit": [{ "command": ".cursor/hooks/format.sh" }]
  }
}
```

Note: Project hooks run from the **project root**, so use `.cursor/hooks/format.sh` (not `./hooks/format.sh`).

Create your hook script at `<project>/.cursor/hooks/format.sh`:

```bash
#!/bin/bash
# Read input, do something, exit 0
cat > /dev/null
exit 0
```

Make it executable:

```bash
chmod +x .cursor/hooks/format.sh
```

Cursor watches hooks config files and reloads them automatically. Your hook runs after every file edit.

## Detailed

### Project hooks (.cursor/)

For project-level hooks that apply to a specific repository, create `<project>/.cursor/hooks.json`:

```json
{
  "version": 1,
  "hooks": {
    "afterFileEdit": [{ "command": ".cursor/hooks/format.sh" }]
  }
}
```

Note: Project hooks run from the **project root**, so use `.cursor/hooks/format.sh` (not `./hooks/format.sh`).

Create your hook script at `<project>/.cursor/hooks/format.sh`:

```bash
#!/bin/bash
# Read input, do something, exit 0
cat > /dev/null
exit 0
```

Make it executable:

```bash
chmod +x .cursor/hooks/format.sh
```

Cursor watches hooks config files and reloads them automatically. Your hook runs after every file edit.

## Provenance

Section "Hooks > Quickstart > Project hooks (.cursor/)" of https://cursor.com/docs/hooks (parent 5f6188ddb87cb68f4a982b8392650fc7591cfe85021b4dcce56657c421800b46)