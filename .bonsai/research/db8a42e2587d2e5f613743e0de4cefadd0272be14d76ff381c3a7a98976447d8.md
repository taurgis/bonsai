---
schema_version: 1
artifact_type: section
source_url: https://en.wikipedia.org/wiki/Cache_replacement_policies#policies-approximating-bldys-algorithm
source_urls:
  - https://en.wikipedia.org/wiki/Cache_replacement_policies#policies-approximating-bldys-algorithm
normalized_url: https://en.wikipedia.org/wiki/Cache_replacement_policies
cache_key: db8a42e2587d2e5f613743e0de4cefadd0272be14d76ff381c3a7a98976447d8
topic: 
tags:
  - cache
  - identifier
  - replacement
  - algorithm
  - edit
format_available:
  - compressed
  - detailed
tier: stable
ttl: 
fetched_at: 2026-06-25T11:16:45.135Z
validated_at: 2026-06-25T11:16:45.135Z
stale_after: 2026-12-22T11:16:45.135Z
capture_method: static_fetch
extraction_status: extracted
extraction_confidence: high
quality_notes:
  - readability extracted main article
  - quality:oversized
  - quality:index-hub
  - auto-generated tags via keyword extraction
supplied_at: 
supplied_by: 
etag: 
last_modified: Wed, 17 Jun 2026 17:38:01 GMT
content_hash: 6da589693253c9d495a261fef0adc9e4379dbcc76830bee6757cc9baa1fa89f4
token_estimate:
  compressed: 693
  detailed: 1028
status: active
site_module_id: 
docs_engine: generated-static
docs_framework: 
source_doc_url: 
search_provider: 
parent_cache_key: 9c1c5c1183276fb17f6c374dfc0a5996c1068c79aa9ed404b1ad05b721ae2e27
section_anchor: policies-approximating-bldys-algorithm
section_heading_path: Policies approximating Bélády's algorithm
---

## Summary

Policies approximating Bélády's algorithm

## Compressed

### Policies approximating Bélády's algorithm

\[[edit]\]

Bélády's algorithm is the optimal cache replacement policy, but it requires knowledge of the future to evict lines that will be reused farthest in the future. A number of replacement policies have been proposed which attempt to predict future reuse distances from past access patterns,[\[22\]] allowing them to approximate the optimal replacement policy. Some of the best-performing cache replacement policies attempt to imitate Bélády's algorithm.

Hawkeye[\[19\]] attempts to emulate Bélády's algorithm by using past accesses by a PC to predict whether the accesses it produces generate cache-friendly (used later) or cache-averse accesses (not used later). It samples a number of non-aligned cache sets, uses a history of length  and emulates Bélády's algorithm on these accesses. This allows the policy to determine which lines should have been cached and which should not, predicting whether an instruction is cache-friendly or cache-averse. This data is then fed into an RRIP; accesses from cache-friendly instructions have a lower RRPV value (likely to be evicted later), and accesses from cache-averse instructions have a higher RRPV value (likely to be evicted sooner). The RRIP backend makes the eviction decisions. The sampled cache and [OPT] generator set the initial RRPV value of the inserted cache lines. Hawkeye won the CRC2 cache championship in 2017,[\[23\]] and Harmony[\[24\]] is an extension of Hawkeye which improves prefetching performance.

[]

Block diagram of the Mockingjay cache replacement policy

Mockingjay[\[25\]] tries to improve on Hawkeye in several ways. It drops the binary prediction, allowing it to make more fine-grained decisions about which cache lines to evict, and leaves the decision about which cache line to evict for when more information is available.

Mockingjay keeps a sampled cache of unique accesses, the PCs that produced them, and their timestamps. When a line in the sampled cache is accessed again, the time difference will be sent to the reuse distance predictor. The RDP uses [temporal difference learning],[\[26\]] where the new RDP value will be increased or decreased by a small number to compensate for outliers; the number is calculated as . If the value has not been initialized, the observed reuse distance is inserted directly. If the sampled cache is full and a line needs to be discarded, the RDP is instructed that the PC that last accessed it produces streaming accesses.

On an access or insertion, the estimated time of reuse (ETR) for this line is updated to reflect the predicted reuse distance. On a cache miss, the line with the highest ETR value is evicted. Mockingjay has results which are close to the optimal Bélády's algorithm.

## Detailed

### Policies approximating Bélády's algorithm

\[[edit](https://en.wikipedia.org/w/index.php?title=Cache_replacement_policies&action=edit&section=27 "Edit section: Policies approximating Bélády's algorithm")\]

Bélády's algorithm is the optimal cache replacement policy, but it requires knowledge of the future to evict lines that will be reused farthest in the future. A number of replacement policies have been proposed which attempt to predict future reuse distances from past access patterns,[\[22\]](https://en.wikipedia.org/wiki/Cache_replacement_policies#cite_note-22) allowing them to approximate the optimal replacement policy. Some of the best-performing cache replacement policies attempt to imitate Bélády's algorithm.

Hawkeye[\[19\]](https://en.wikipedia.org/wiki/Cache_replacement_policies#cite_note-:1-19) attempts to emulate Bélády's algorithm by using past accesses by a PC to predict whether the accesses it produces generate cache-friendly (used later) or cache-averse accesses (not used later). It samples a number of non-aligned cache sets, uses a history of length ![{\\displaystyle 8\\times {\\text{the cache size}}}](https://wikimedia.org/api/rest_v1/media/math/render/svg/50d5cbe8f2d6db230e724d98ebea3a68c5721bf4) and emulates Bélády's algorithm on these accesses. This allows the policy to determine which lines should have been cached and which should not, predicting whether an instruction is cache-friendly or cache-averse. This data is then fed into an RRIP; accesses from cache-friendly instructions have a lower RRPV value (likely to be evicted later), and accesses from cache-averse instructions have a higher RRPV value (likely to be evicted sooner). The RRIP backend makes the eviction decisions. The sampled cache and [OPT](https://en.wikipedia.org/wiki/Page_replacement_algorithm#The_theoretically_optimal_page_replacement_algorithm "Page replacement algorithm") generator set the initial RRPV value of the inserted cache lines. Hawkeye won the CRC2 cache championship in 2017,[\[23\]](https://en.wikipedia.org/wiki/Cache_replacement_policies#cite_note-23) and Harmony[\[24\]](https://en.wikipedia.org/wiki/Cache_replacement_policies#cite_note-24) is an extension of Hawkeye which improves prefetching performance.

[![See caption](//upload.wikimedia.org/wikipedia/commons/thumb/5/5f/Mockingjay_Description.svg/250px-Mockingjay_Description.svg.png)](https://en.wikipedia.org/wiki/File:Mockingjay_Description.svg)

Block diagram of the Mockingjay cache replacement policy

Mockingjay[\[25\]](https://en.wikipedia.org/wiki/Cache_replacement_policies#cite_note-25) tries to improve on Hawkeye in several ways. It drops the binary prediction, allowing it to make more fine-grained decisions about which cache lines to evict, and leaves the decision about which cache line to evict for when more information is available.

Mockingjay keeps a sampled cache of unique accesses, the PCs that produced them, and their timestamps. When a line in the sampled cache is accessed again, the time difference will be sent to the reuse distance predictor. The RDP uses [temporal difference learning](https://en.wikipedia.org/wiki/Temporal_difference_learning "Temporal difference learning"),[\[26\]](https://en.wikipedia.org/wiki/Cache_replacement_policies#cite_note-26) where the new RDP value will be increased or decreased by a small number to compensate for outliers; the number is calculated as ![{\\displaystyle w=\\min \\left(1,{\\frac {\\text{timestamp difference}}{16}}\\right)}](https://wikimedia.org/api/rest_v1/media/math/render/svg/a657fdf8590e6301dc0a80234844e75a281c8c64). If the value has not been initialized, the observed reuse distance is inserted directly. If the sampled cache is full and a line needs to be discarded, the RDP is instructed that the PC that last accessed it produces streaming accesses.

On an access or insertion, the estimated time of reuse (ETR) for this line is updated to reflect the predicted reuse distance. On a cache miss, the line with the highest ETR value is evicted. Mockingjay has results which are close to the optimal Bélády's algorithm.

## Provenance

Section "Policies approximating Bélády's algorithm" of https://en.wikipedia.org/wiki/Cache_replacement_policies (parent 9c1c5c1183276fb17f6c374dfc0a5996c1068c79aa9ed404b1ad05b721ae2e27)