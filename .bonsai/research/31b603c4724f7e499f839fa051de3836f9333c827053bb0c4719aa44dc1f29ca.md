---
schema_version: 1
artifact_type: section
source_url: https://en.wikipedia.org/wiki/Cache_replacement_policies#simple-frequency-based-policies
source_urls:
  - https://en.wikipedia.org/wiki/Cache_replacement_policies#simple-frequency-based-policies
normalized_url: https://en.wikipedia.org/wiki/Cache_replacement_policies
cache_key: 31b603c4724f7e499f839fa051de3836f9333c827053bb0c4719aa44dc1f29ca
topic: 
tags:
  - cache
  - identifier
  - replacement
  - algorithm
  - edit
format_available:
  - compressed
  - detailed
tier: stable
ttl: 
fetched_at: 2026-06-25T11:16:45.135Z
validated_at: 2026-06-25T11:16:45.135Z
stale_after: 2026-12-22T11:16:45.135Z
capture_method: static_fetch
extraction_status: extracted
extraction_confidence: high
quality_notes:
  - readability extracted main article
  - quality:oversized
  - quality:index-hub
  - auto-generated tags via keyword extraction
supplied_at: 
supplied_by: 
etag: 
last_modified: Wed, 17 Jun 2026 17:38:01 GMT
content_hash: 3666391afc0d781a383bbbb0a27d93e6b993320e003cb457f3753b1deabba737
token_estimate:
  compressed: 740
  detailed: 1012
status: active
site_module_id: 
docs_engine: generated-static
docs_framework: 
source_doc_url: 
search_provider: 
parent_cache_key: 9c1c5c1183276fb17f6c374dfc0a5996c1068c79aa9ed404b1ad05b721ae2e27
section_anchor: simple-frequency-based-policies
section_heading_path: Simple frequency-based policies
---

## Summary

Simple frequency-based policies

## Compressed

### Simple frequency-based policies

\[[edit]\]

#### Least frequently used (LFU)

\[[edit]")\]

The LFU algorithm counts how often an item is needed; those used less often are discarded first. This is similar to LRU, except that how many times a block was accessed is stored instead of how recently. While running an access sequence, the block which was used the fewest times will be removed from the cache.

#### Least frequent recently used (LFRU)

\[[edit]")\]

The least frequent recently used (LFRU)[\[16\]] algorithm combines the benefits of LFU and LRU. LFRU is suitable for network cache applications such as [ICN], [CDNs], and distributed networks in general. In LFRU, the cache is divided into two partitions: privileged and unprivileged. The privileged partition is protected; if content is popular, it is pushed into the privileged partition. In replacing the privileged partition, LFRU evicts content from the unprivileged partition; pushes content from the privileged to the unprivileged partition, and inserts new content into the privileged partition. LRU is used for the privileged partition and an approximated LFU (ALFU) algorithm for the unprivileged partition.

#### LFU with dynamic aging (LFUDA)

\[[edit]")\]

A variant, LFU with dynamic aging (LFUDA), uses dynamic aging to accommodate shifts in a set of popular objects; it adds a cache-age factor to the [reference count] when a new object is added to the cache or an existing object is re-referenced. LFUDA increments cache age when evicting blocks by setting it to the evicted object's key value, and the cache age is always less than or equal to the minimum key value in the cache.[\[17\]] If an object was frequently accessed in the past and becomes unpopular, it will remain in the cache for a long time (preventing newly- or less-popular objects from replacing it). Dynamic aging reduces the number of such objects, making them eligible for replacement, and LFUDA reduces [cache pollution] caused by LFU when a cache is small.

This is a new eviction algorithm designed in 2023. Compared to existing algorithms, which mostly build on LRU (least-recently-used), S3-FIFO only uses three FIFO queues: a small queue occupying 10% of cache space, a main queue that uses 90% of the cache space, and a ghost queue that only stores object metadata. The small queue is used to filter out one-hit-wonders (objects that are only accessed once in a short time window); the main queue is used to store popular objects and uses reinsertion to keep them in the cache; and the ghost queue is used to catch potentially-popular objects that are evicted from the small queue. Objects are first inserted into the small queue (if they are not found in the ghost queue, otherwise inserted into the main queue); upon eviction from the small queue, if an object has been requested, it is reinserted into the main queue, otherwise, it is evicted and the metadata is tracked in the ghost queue.[\[18\]]

## Detailed

### Simple frequency-based policies

\[[edit](https://en.wikipedia.org/w/index.php?title=Cache_replacement_policies&action=edit&section=17 "Edit section: Simple frequency-based policies")\]

#### Least frequently used (LFU)

\[[edit](https://en.wikipedia.org/w/index.php?title=Cache_replacement_policies&action=edit&section=18 "Edit section: Least frequently used (LFU)")\]

The LFU algorithm counts how often an item is needed; those used less often are discarded first. This is similar to LRU, except that how many times a block was accessed is stored instead of how recently. While running an access sequence, the block which was used the fewest times will be removed from the cache.

#### Least frequent recently used (LFRU)

\[[edit](https://en.wikipedia.org/w/index.php?title=Cache_replacement_policies&action=edit&section=19 "Edit section: Least frequent recently used (LFRU)")\]

The least frequent recently used (LFRU)[\[16\]](https://en.wikipedia.org/wiki/Cache_replacement_policies#cite_note-16) algorithm combines the benefits of LFU and LRU. LFRU is suitable for network cache applications such as [ICN](https://en.wikipedia.org/wiki/Information-centric_networking "Information-centric networking"), [CDNs](https://en.wikipedia.org/wiki/Content_delivery_network "Content delivery network"), and distributed networks in general. In LFRU, the cache is divided into two partitions: privileged and unprivileged. The privileged partition is protected; if content is popular, it is pushed into the privileged partition. In replacing the privileged partition, LFRU evicts content from the unprivileged partition; pushes content from the privileged to the unprivileged partition, and inserts new content into the privileged partition. LRU is used for the privileged partition and an approximated LFU (ALFU) algorithm for the unprivileged partition.

#### LFU with dynamic aging (LFUDA)

\[[edit](https://en.wikipedia.org/w/index.php?title=Cache_replacement_policies&action=edit&section=20 "Edit section: LFU with dynamic aging (LFUDA)")\]

A variant, LFU with dynamic aging (LFUDA), uses dynamic aging to accommodate shifts in a set of popular objects; it adds a cache-age factor to the [reference count](https://en.wikipedia.org/wiki/Reference_counting "Reference counting") when a new object is added to the cache or an existing object is re-referenced. LFUDA increments cache age when evicting blocks by setting it to the evicted object's key value, and the cache age is always less than or equal to the minimum key value in the cache.[\[17\]](https://en.wikipedia.org/wiki/Cache_replacement_policies#cite_note-17) If an object was frequently accessed in the past and becomes unpopular, it will remain in the cache for a long time (preventing newly- or less-popular objects from replacing it). Dynamic aging reduces the number of such objects, making them eligible for replacement, and LFUDA reduces [cache pollution](https://en.wikipedia.org/wiki/Cache_pollution "Cache pollution") caused by LFU when a cache is small.

This is a new eviction algorithm designed in 2023. Compared to existing algorithms, which mostly build on LRU (least-recently-used), S3-FIFO only uses three FIFO queues: a small queue occupying 10% of cache space, a main queue that uses 90% of the cache space, and a ghost queue that only stores object metadata. The small queue is used to filter out one-hit-wonders (objects that are only accessed once in a short time window); the main queue is used to store popular objects and uses reinsertion to keep them in the cache; and the ghost queue is used to catch potentially-popular objects that are evicted from the small queue. Objects are first inserted into the small queue (if they are not found in the ghost queue, otherwise inserted into the main queue); upon eviction from the small queue, if an object has been requested, it is reinserted into the main queue, otherwise, it is evicted and the metadata is tracked in the ghost queue.[\[18\]](https://en.wikipedia.org/wiki/Cache_replacement_policies#cite_note-18)

## Provenance

Section "Simple frequency-based policies" of https://en.wikipedia.org/wiki/Cache_replacement_policies (parent 9c1c5c1183276fb17f6c374dfc0a5996c1068c79aa9ed404b1ad05b721ae2e27)