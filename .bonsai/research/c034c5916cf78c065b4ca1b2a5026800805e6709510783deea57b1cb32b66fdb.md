---
schema_version: 1
artifact_type: source
source_url: https://oclif.io/docs/commands/
source_urls:
  - https://oclif.io/docs/commands/
normalized_url: https://oclif.io/docs/commands/
cache_key: c034c5916cf78c065b4ca1b2a5026800805e6709510783deea57b1cb32b66fdb
topic: 
tags:
  - command
  - direct
  - link
  - error
  - methods
format_available:
  - compressed
  - detailed
tier: standard
ttl: 
fetched_at: 2026-06-27T10:57:47.054Z
validated_at: 2026-06-27T10:57:47.054Z
stale_after: 2026-07-27T10:57:47.054Z
capture_method: static_fetch
extraction_status: extracted
extraction_confidence: high
quality_notes:
  - readability extracted main article
  - auto-generated tags via keyword extraction
supplied_at: 
supplied_by: 
etag: W/"6a37a275-fbd8"
last_modified: Sun, 21 Jun 2026 08:36:05 GMT
content_hash: 3c33b46853f962dcb12ab54583051f00aaaaec998a88ed6ae2764578b476ff01
token_estimate:
  compressed: 1265
  detailed: 1955
status: active
site_module_id: 
docs_engine: docusaurus
docs_framework: 
source_doc_url: 
search_provider: algolia-docsearch
parent_cache_key: 
section_anchor: 
section_heading_path: 
---

## Summary

Commands | oclif: The Open CLI Framework

## Compressed

A basic command looks like the following in TypeScript:

```js
import {Command} from '@oclif/core'

export class MyCommand extends Command {
  static description = 'description of this example command'

  async run(): Promise<void> {
    console.log('running my command')
  }
}
```

The only part that is required is the run function.

In JavaScript:

```js
const {Command} = require('@oclif/core')

class MyCommand extends Command {
  async run() {
    console.log('running my command')
  }
}

MyCommand.description = 'description of this example command'

module.exports = MyCommand
```

Note that the following examples will be in TypeScript.

### Avoiding Timeouts[​]

In order to avoid command executions running indefinitely, oclif will terminate the node process 10 seconds after `Command.run` resolves.

In other words, **if you execute a promise in `Command.run` without a awaiting it, then the command will likely timeout before it's completed.**

### Other Command Options[​]

[See the base class to get an idea of what methods can be called on a command].

```typescript
import {Command, Flags} from '@oclif/core'

export class MyCommand extends Command {
  static summary = 'A brief overview of your command.'
  static description = `
An in-depth description of the command.
It can be multiline.
`

  // hide the command from help
  static hidden = false

  // custom usage string for help
  // this overrides the default usage
  static usage = 'mycommand --myflag'

  // examples to add to help
  // <%= config.bin %> resolves to the executable name
  // <%= command.id %> resolves to the command name
  static examples = [
    // Examples can be simple strings
    '<%= config.bin %> <%= command.id %> --help',
    // Or objects that provide a description of the example command
    {
      description: 'Force the command to execute',
      command: '<%= config.bin %> <%= command.id %> --force',
    }
  ]

  // this makes the parser not fail when it receives invalid arguments
  // defaults to true
  // set it to false if you need to accept a variable number of arguments
  static strict = false

  // define aliases that can execute this command.
  static aliases = ['alternate:name:for:this:command']

  // Set to true if you want to add the --json flag to your command.
  // oclif will automatically suppress logs (if you use this.log, this.warn, or this.error) and
  // display the JSON returned by the command's run method.
  static enableJsonFlag = true

  async run() {
    // show a warning
    this.warn('uh oh!')
    // exit with an error message
    this.error('uh oh!!!')
    // exit with status code
    this.exit(1)
  }
}
```

## Command Methods[​]

*   [Command Methods]
    *   [`this.log(message: string)`]
    *   [`this.warn(message: string | Error)`]
    *   [`this.error(message: string | Error, options?: {code?: string, exit?: number, ref?: string; suggestions?: string[];})`]
    *   [`this.exit(code: number = 0)`]
    *   [`this.logToStderr(message: string)`]
    *   [`this.jsonEnabled()`]
    *   [`this.toSuccessJson(result: unknown)`]
    *   [`this.toErrorJson(result: unknown)`]

The following assumes you are in the `run()` method of an oclif [command].

### `this.log(message: string)`[​]

Output message to stdout (non-blocking). `console.log()` works fine too, but that is a blocking call and won't be automatically suppressed when the `--json` flag is present.

```typescript
this.log('hello, world!')
```

### `this.warn(message: string | Error)`[​]

Display an error or message as a warning

```typescript
this.warn('uh oh!')
this.warn(new Error('uh oh!'))
```

### `this.error(message: string | Error, options?: {code?: string, exit?: number, ref?: string; suggestions?: string[];})`[​]

Display error and exit.

```typescript
this.error('uh oh!', {exit: 2})
this.error(new Error('uh oh!'))
```

The options object has the following options:

*   `exit` — exit code to use
*   `code` — a unique error code for the type of error
*   `suggestions` — an array of suggestions for a user to try next that may be useful or provide additional context
*   `ref` — a url to documentation related to this error or fixing it

The `message`, `code`, `suggestions`, `ref` properties will be displayed when an error is shown.

These errors are friendly and won't show a traceback unless debugging is enabled with `DEBUG=*`.

```typescript
import {CLIError} from '@oclif/errors'

throw new CLIError('my friendly error')
```

Any error caught by the command of this `CLIError` type will be shown without traceback.

### `this.exit(code: number = 0)`[​]

Exit process.

### `this.logToStderr(message: string)`[​]

Log a message to the terminal's `stderr`.

### `this.jsonEnabled()`[​]

Returns to `true` if the `--json` flag is present and `enableJsonFlag` is set to `true`

### `this.toSuccessJson(result: unknown)`[​]

Modify the command's success JSON output before it's displayed to the user.

### `this.toErrorJson(result: unknown)`[​]

Modify the command's error JSON output before it's displayed to the user.

## Detailed

A basic command looks like the following in TypeScript:

```js
import {Command} from '@oclif/core'

export class MyCommand extends Command {
  static description = 'description of this example command'

  async run(): Promise<void> {
    console.log('running my command')
  }
}
```

The only part that is required is the run function. Accept user input with [arguments](https://oclif.io/docs/args) and [flags](https://oclif.io/docs/flags).

In JavaScript:

```js
const {Command} = require('@oclif/core')

class MyCommand extends Command {
  async run() {
    console.log('running my command')
  }
}

MyCommand.description = 'description of this example command'

module.exports = MyCommand
```

Note that the following examples will be in TypeScript. As JavaScript does not yet have static class properties, you will have to add them to the class after it is declared like we did with the description above.

### Avoiding Timeouts[​](https://oclif.io/docs/commands#avoiding-timeouts "Direct link to Avoiding Timeouts")

In order to avoid command executions running indefinitely, oclif will terminate the node process 10 seconds after `Command.run` resolves. This means that all command logic inside the `run` method should either run synchronously or should return a `Promise`. This will allow the entire command to run before the 10 second timeout starts.

In other words, **if you execute a promise in `Command.run` without a awaiting it, then the command will likely timeout before it's completed.**

### Other Command Options[​](https://oclif.io/docs/commands#other-command-options "Direct link to Other Command Options")

[See the base class to get an idea of what methods can be called on a command](https://github.com/oclif/core/blob/main/src/command.ts).

```typescript
import {Command, Flags} from '@oclif/core'

export class MyCommand extends Command {
  static summary = 'A brief overview of your command.'
  static description = `
An in-depth description of the command.
It can be multiline.
`

  // hide the command from help
  static hidden = false

  // custom usage string for help
  // this overrides the default usage
  static usage = 'mycommand --myflag'

  // examples to add to help
  // <%= config.bin %> resolves to the executable name
  // <%= command.id %> resolves to the command name
  static examples = [
    // Examples can be simple strings
    '<%= config.bin %> <%= command.id %> --help',
    // Or objects that provide a description of the example command
    {
      description: 'Force the command to execute',
      command: '<%= config.bin %> <%= command.id %> --force',
    }
  ]

  // this makes the parser not fail when it receives invalid arguments
  // defaults to true
  // set it to false if you need to accept a variable number of arguments
  static strict = false

  // define aliases that can execute this command.
  static aliases = ['alternate:name:for:this:command']

  // Set to true if you want to add the --json flag to your command.
  // oclif will automatically suppress logs (if you use this.log, this.warn, or this.error) and
  // display the JSON returned by the command's run method.
  static enableJsonFlag = true

  async run() {
    // show a warning
    this.warn('uh oh!')
    // exit with an error message
    this.error('uh oh!!!')
    // exit with status code
    this.exit(1)
  }
}
```

## Command Methods[​](https://oclif.io/docs/commands#command-methods "Direct link to Command Methods")

*   [Command Methods](https://oclif.io/docs/commands#command-methods)
    *   [`this.log(message: string)`](https://oclif.io/docs/commands#thislogmessage-string)
    *   [`this.warn(message: string | Error)`](https://oclif.io/docs/commands#thiswarnmessage-string--error)
    *   [`this.error(message: string | Error, options?: {code?: string, exit?: number, ref?: string; suggestions?: string[];})`](https://oclif.io/docs/commands#thiserrormessage-string--error-options-code-string-exit-number-ref-string-suggestions-string)
    *   [`this.exit(code: number = 0)`](https://oclif.io/docs/commands#thisexitcode-number--0)
    *   [`this.logToStderr(message: string)`](https://oclif.io/docs/commands#thislogtostderrmessage-string)
    *   [`this.jsonEnabled()`](https://oclif.io/docs/commands#thisjsonenabled)
    *   [`this.toSuccessJson(result: unknown)`](https://oclif.io/docs/commands#thistosuccessjsonresult-unknown)
    *   [`this.toErrorJson(result: unknown)`](https://oclif.io/docs/commands#thistoerrorjsonresult-unknown)

The following assumes you are in the `run()` method of an oclif [command](https://oclif.io/docs/commands).

### `this.log(message: string)`[​](https://oclif.io/docs/commands#thislogmessage-string "Direct link to thislogmessage-string")

Output message to stdout (non-blocking). `console.log()` works fine too, but that is a blocking call and won't be automatically suppressed when the `--json` flag is present. This uses [util.format()](https://nodejs.org/api/util.html#util_util_format_format_args) which behaves the same as `console.log()`.

```typescript
this.log('hello, world!')
```

### `this.warn(message: string | Error)`[​](https://oclif.io/docs/commands#thiswarnmessage-string--error "Direct link to thiswarnmessage-string--error")

Display an error or message as a warning

```typescript
this.warn('uh oh!')
this.warn(new Error('uh oh!'))
```

### `this.error(message: string | Error, options?: {code?: string, exit?: number, ref?: string; suggestions?: string[];})`[​](https://oclif.io/docs/commands#thiserrormessage-string--error-options-code-string-exit-number-ref-string-suggestions-string "Direct link to thiserrormessage-string--error-options-code-string-exit-number-ref-string-suggestions-string")

Display error and exit. Also add a code to error object or exit status.

```typescript
this.error('uh oh!', {exit: 2})
this.error(new Error('uh oh!'))
```

The options object has the following options:

*   `exit` — exit code to use
*   `code` — a unique error code for the type of error
*   `suggestions` — an array of suggestions for a user to try next that may be useful or provide additional context
*   `ref` — a url to documentation related to this error or fixing it

The `message`, `code`, `suggestions`, `ref` properties will be displayed when an error is shown. Reusable `Error` classes can be created that display the optional outputs above by implementing the `PrettyPrintableError` interface from the `Errors` namespace from `@oclif/core` and `this.error` will handle them appropriately.

These errors are friendly and won't show a traceback unless debugging is enabled with `DEBUG=*`.

```typescript
import {CLIError} from '@oclif/errors'

throw new CLIError('my friendly error')
```

Any error caught by the command of this `CLIError` type will be shown without traceback.

### `this.exit(code: number = 0)`[​](https://oclif.io/docs/commands#thisexitcode-number--0 "Direct link to thisexitcode-number--0")

Exit process. Defaults to status 0.

### `this.logToStderr(message: string)`[​](https://oclif.io/docs/commands#thislogtostderrmessage-string "Direct link to thislogtostderrmessage-string")

Log a message to the terminal's `stderr`.

### `this.jsonEnabled()`[​](https://oclif.io/docs/commands#thisjsonenabled "Direct link to thisjsonenabled")

Returns to `true` if the `--json` flag is present and `enableJsonFlag` is set to `true`

### `this.toSuccessJson(result: unknown)`[​](https://oclif.io/docs/commands#thistosuccessjsonresult-unknown "Direct link to thistosuccessjsonresult-unknown")

Modify the command's success JSON output before it's displayed to the user.

### `this.toErrorJson(result: unknown)`[​](https://oclif.io/docs/commands#thistoerrorjsonresult-unknown "Direct link to thistoerrorjsonresult-unknown")

Modify the command's error JSON output before it's displayed to the user.

## Provenance

Fetched from https://oclif.io/docs/commands/ on 2026-06-27T10:57:47.054Z