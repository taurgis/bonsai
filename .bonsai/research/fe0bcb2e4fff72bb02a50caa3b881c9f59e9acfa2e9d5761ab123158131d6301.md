---
schema_version: 1
artifact_type: section
source_url: https://prismjs.com/extending.html#fcfs-matchinghttpsprismjscomextendinghtmlfcfs-matching
source_urls:
  - https://prismjs.com/extending.html#fcfs-matchinghttpsprismjscomextendinghtmlfcfs-matching
normalized_url: https://prismjs.com/extending.html
cache_key: fe0bcb2e4fff72bb02a50caa3b881c9f59e9acfa2e9d5761ab123158131d6301
topic: 
tags:
  - token
  - language
  - tokens
  - greedy
  - matching
format_available:
  - compressed
  - detailed
tier: standard
ttl: 
fetched_at: 2026-06-25T12:22:04.560Z
validated_at: 2026-06-25T12:22:04.560Z
stale_after: 2026-07-25T12:22:04.560Z
capture_method: static_fetch
extraction_status: extracted
extraction_confidence: high
quality_notes:
  - readability extracted main article
  - auto-generated tags via keyword extraction
supplied_at: 
supplied_by: 
etag: "75c1be9c00a10c790ff8f1b3cee18f33-ssl-df"
last_modified: 
content_hash: be356952738788c5bd762218978cc0829f5f0bf686f398f311192c923e24927f
token_estimate:
  compressed: 367
  detailed: 509
status: active
site_module_id: 
docs_engine: generated-static
docs_framework: 
source_doc_url: 
search_provider: 
parent_cache_key: 3fab6a2f2f26adaf2e39632f24c88fa3bdde57a39e3fd5ff168bc946210edc59
section_anchor: fcfs-matchinghttpsprismjscomextendinghtmlfcfs-matching
section_heading_path: [The matching algorithm](https://prismjs.com/extending.html#the-matching-algorithm) > [FCFS matching](https://prismjs.com/extending.html#fcfs-matching)
---

## Summary

[The matching algorithm](https://prismjs.com/extending.html#the-matching-algorithm) > [FCFS matching](https://prismjs.com/extending.html#fcfs-matching)

## Compressed

### [FCFS matching]

This is Prism default matching mode. All tokens are matched one after the other, in order, tokens cannot overlap, and tokens cannot match text that is already matched by previous tokens.

The algorithm itself is quite simple.

```
[
	["function", "max"],
	"(3, 5, ",
	["function", "exp2"],
	"(7));"
]
```

Next, we would tokenize numbers with the token `'number': /[0-9]+/`.

FCFS matching will go through all strings in the current token stream to find matches for the number regex. A new token is created for `3` and inserted into the token stream to replace the matching text.

```
[
	["function", "max"],
	"(",
	["number", "3"],
	", 5, ",
	["function", "exp2"],
	"(7));"
]
```

Now, the algorithm goes to the next string `", 5, "` and finds another match.

```
[
	["function", "max"],
	"(",
	["number", "3"],
	", ",
	["number", "5"],
	", ",
	["function", "exp2"],
	"(7));"
]
```

The next string is `", "` and no matches are found.

```
[
	["function", "max"],
	"(",
	["number", "3"],
	", ",
	["number", "5"],
	", ",
	["function", "exp2"],
	"(",
	["number", "7"],
	"));"
]
```

The last string to check is `"));"` and no matches are found. The number token has now been processed and the algorithm will go process the next token in the language definition.

Notice how FCFS matching did not find the `2` in `exp2`. Since FCFS matching completely ignores existing tokens in the token stream, the number regex cannot see already-tokenized text.

## Detailed

### [FCFS matching](https://prismjs.com/extending.html#fcfs-matching)

This is Prism default matching mode. All tokens are matched one after the other, in order, tokens cannot overlap, and tokens cannot match text that is already matched by previous tokens.

The algorithm itself is quite simple. Let’s say we wanted to tokenize the JS code `max(3, 5, exp2(7));` and that function tokens had already been processed. The current token stream would be:

```
[
	["function", "max"],
	"(3, 5, ",
	["function", "exp2"],
	"(7));"
]
```

Next, we would tokenize numbers with the token `'number': /[0-9]+/`.

FCFS matching will go through all strings in the current token stream to find matches for the number regex. The first string is `"(3, 5, "`, so the match `3` is found. A new token is created for `3` and inserted into the token stream to replace the matching text. The token stream is now:

```
[
	["function", "max"],
	"(",
	["number", "3"],
	", 5, ",
	["function", "exp2"],
	"(7));"
]
```

Now, the algorithm goes to the next string `", 5, "` and finds another match. A new token is created for `5` and the token stream is now:

```
[
	["function", "max"],
	"(",
	["number", "3"],
	", ",
	["number", "5"],
	", ",
	["function", "exp2"],
	"(7));"
]
```

The next string is `", "` and no matches are found. The string after that is `"(7));"` and a new token is create for `7`:

```
[
	["function", "max"],
	"(",
	["number", "3"],
	", ",
	["number", "5"],
	", ",
	["function", "exp2"],
	"(",
	["number", "7"],
	"));"
]
```

The last string to check is `"));"` and no matches are found. The number token has now been processed and the algorithm will go process the next token in the language definition.

Notice how FCFS matching did not find the `2` in `exp2`. Since FCFS matching completely ignores existing tokens in the token stream, the number regex cannot see already-tokenized text. This is a very useful property. In the above example, `2` is a part of the function name `exp2`, so highlighting it as a number would be incorrect.

## Provenance

Section "[The matching algorithm](https://prismjs.com/extending.html#the-matching-algorithm) > [FCFS matching](https://prismjs.com/extending.html#fcfs-matching)" of https://prismjs.com/extending.html (parent 3fab6a2f2f26adaf2e39632f24c88fa3bdde57a39e3fd5ff168bc946210edc59)