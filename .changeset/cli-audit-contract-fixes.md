---
'@taurgis/bonsai': patch
---

Fix CLI contract and UX gaps found in a full happy/unhappy-path audit: `-l` before URL shorthand, localhost cache keys, status `--tier`, prune filter semantics, inspect multi-URL payloads, and stable `MISSING_COMMAND` / suggestion envelopes.
