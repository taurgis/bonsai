---
'@taurgis/bonsai': patch
---

Follow-up CLI structural cleanup: split emit build vs write/exit, invert argv flag ownership via a composition-root manifest, move write-status helpers out of research persist, share config set/unset persist, extract fetch/import application services, and type list/prune/inspect row models.
